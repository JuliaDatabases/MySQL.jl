var documenterSearchIndex = {"docs":
[{"category":"section","location":"migration/#Migrating-from-1.x-to-2.0","page":"Migrating from 1.x","text":"MySQL.jl 2.0 replaces the MariaDB Connector/C backend with a native wire-protocol implementation: the MySQL client/server protocol written in Julia on top of Reseau transports (TCP and TLS). It is not \"pure Julia\" — OpenSSL underpins TLS and the RSA password exchange — but every libmariadb ccall, its dynamic plugin loading, and its C handle lifetimes are gone, along with the crash classes they caused (issues #220, #236, #240, #208, #206).\n\nMySQL.Connection is now the native connection. Most code — DBInterface.connect / execute / prepare / executemany / executemultiple / transaction, Tables.jl cursors, MySQL.load, buffered (mysql_store_result=true, the default) and streaming result sets — works unchanged. Pin MySQL = \"1\" to stay on the last Connector/C release.","title":"Migrating from 1.x to 2.0"},{"category":"section","location":"migration/#Upgrade-checklist","page":"Migrating from 1.x","text":"MySQL.jl 2.0 requires Julia 1.10 or later.\n\nErrors: replace MySQL.API.Error / MySQL.API.StmtError with MySQL.Error / MySQL.StmtError (same field names/types plus a new sqlstate), or catch the root MySQL.MySQLError.\nValue types: replace MySQL.API.Bit with MySQL.Bit. Text columns decode to DataString and BLOB columns to DataBytes (DataStrings.jl): zero-copy views of the result buffer that compare, hash, sort, and print like String/Vector{UInt8}; copy out with String(s)/Vector{UInt8}(b), or ask the typed accessor for String explicitly. Code that dispatches on ::String needs ::AbstractString. DATETIME/TIMESTAMP columns decode to Timestamp{P} — Durations.jl's Int64 Unix-epoch instant, the type proposed for the Julia 1.14 Dates stdlib, which MySQL re-exports — at the column's declared fractional precision: DATETIME is Timestamp{Second}, DATETIME(1..3) Timestamp{Millisecond}, DATETIME(4..6) Timestamp{Microsecond}. Every value is exact, so MySQL.DateAndTime and the mysql_date_and_time keyword are gone. DateTime(ts), Date(ts), and Time(ts) convert; comparisons with DateTime work directly; DateTime values still bind as parameters. DECIMAL results decode to MySQL.DecimalResult (DataDecimals.DecimalValue{DataDecimals.Int256}), which preserves all 65 digits and the stored scale, instead of DecFP.Dec64; DataDecimals values bind directly as parameters, and MySQL.load infers DECIMAL(P,S) from a fixed-scale type (for a DecimalValue column, specify the destination type with coltypes). DecFP is not supported anymore: convert Dec64/Dec128 values to DataDecimals (or strings) before binding them.\nMulti-statements: pass multi_statements=true if you relied on 1.x accepting \"stmt1; stmt2\" by default (an if/elseif bug made 1.x enable it silently).\nEnum-valued options: pass Symbols — ssl_mode=:required (was MySQL.API.SSL_MODE_REQUIRED), protocol=:tcp (was MySQL.API.MYSQL_PROTOCOL_TCP).\nLocal servers: every host, \"localhost\" and \"\" included, is dialed over TCP (the Connector/C rule that turned localhost into a Unix socket is gone). The socket and named-pipe transports are not implemented yet: protocol=:socket/:pipe or named_pipe=true raise a clear error (an explicit protocol=:tcp overrides named_pipe). A unix_socket path is accepted but unused. TCP access also needs a matching account grant. With skip_name_resolve, a 'user'@'localhost' grant need not cover 127.0.0.1 or ::1; see MySQL's host-resolution rules. Socket-only authentication also needs a TCP-compatible account.\nUnknown, removed, or unavailable keywords now error with an explanation instead of being silently swallowed — fix the call sites the errors point at.","title":"Upgrade checklist"},{"category":"section","location":"migration/#The-MySQL.API-module-is-gone","page":"Migrating from 1.x","text":"1.x names and their 2.0 replacements:\n\n1.x 2.0\nMySQL.API.Error, MySQL.API.StmtError MySQL.Error, MySQL.StmtError (aliases of MySQL.Protocol.Error/StmtError; same errno::Cuint/msg fields and showerror text, plus sqlstate); root type MySQL.MySQLError\nMySQL.API.Bit MySQL.Bit (same bits::UInt64 field)\nMySQL.DateAndTime (MySQL.API.DateAndTime), mysql_date_and_time=true removed: DATETIME/TIMESTAMP decode to Timestamp{P} (re-exported from Durations.jl) with every digit the column carries\nMySQL.API.juliatype / MySQL.juliatype MySQL.juliatype (strings → DataString, blobs → DataBytes, DATETIME/TIMESTAMP → Timestamp{P}, DECIMAL → DataDecimals)\nMySQL.API.MYSQL_TYPE_* constants MySQL.Protocol.MYSQL_TYPE_* (wire-value UInt8s)\nMySQL.API.SSL_MODE_*, MySQL.API.MYSQL_PROTOCOL_* enums Symbols: ssl_mode=:disabled/:preferred/:required/:verify_ca/:verify_identity, protocol=:default/:tcp/:socket/:pipe\nMySQL.API.mysqltype removed (parameter types are inferred from Julia values when binding)\nMySQL.API handle types (MYSQL, MYSQL_STMT, MYSQL_RES, MYSQL_BIND), raw ccall wrappers, MySQL.setoptions!/API.getoption removed — there are no C handles\nconn.mysql (the raw C handle field on Connection) removed — use the driver API; per-command results live on the returned cursor (cur.rows_affected, DBInterface.lastrowid(cur))\n\nCursor/row types: MySQL.Cursor{binary, buffered} with aliases MySQL.TextCursor (DBInterface.execute(conn, sql)) and MySQL.BinaryCursor (prepared execution); row types MySQL.TextRow and MySQL.BinaryRow (1.x: MySQL.TextRow and MySQL.Row).","title":"The MySQL.API module is gone"},{"category":"section","location":"migration/#Additional-2.0-breaks-(beyond-the-behavior-table)","page":"Migrating from 1.x","text":"mysql:// stripping: only a leading mysql:// prefix on the host is stripped; 1.x stripped everything up to a mysql:// substring found anywhere in the host.\nport=0 now means the default port (3306); in 1.x it meant \"take the port from the option file\". Omit port (or pass nothing) to fall back to option files.\nconn.port is an Int (was a String), and Base.show prints it unquoted.\nMySQL.load debug keyword accepts false/true/:values; debug=true logs generated statements only, debug=:values also logs row values (1.x debug=true logged values). A DateTime column is created as DATETIME(3) (1.x: DATETIME, which dropped the milliseconds); Timestamp{Second}/{Millisecond}/{Microsecond} columns become DATETIME/DATETIME(3)/DATETIME(6).","title":"Additional 2.0 breaks (beyond the behavior table)"},{"category":"section","location":"migration/#Unchanged-(Preserve)","page":"Migrating from 1.x","text":"The observable 1.x surface is preserved unless a row below says otherwise, including: positional connect(MySQL.Connection, host, user, passwd), passwd=nothing vs \"\", option files (subset; unsupported directives fail closed), init_command, found_rows/no_schema/ignore_space as independent flags, the result type mapping (MySQL.juliatype) except for DECIMAL and the decoding policies below, driver-keyword dispatch on execute (SQL parameters still cannot be passed as keywords), executemany, the wrongrow contract (\"a row is only valid while it is the cursor's current row\", same ArgumentError), rows_affected::Int64 bitcast semantics, cursor close!/close idempotence, and escaping (MySQL.escape).\n\nThese are asserted by the executable behavior manifest (test/behavior_manifest.jl), whose golden values were captured from dual-backend runs against the same servers before the C backend was removed.","title":"Unchanged (Preserve)"},{"category":"section","location":"migration/#Behavior-changes-(Fix)","page":"Migrating from 1.x","text":"Deliberate, documented changes relative to Connector/C 1.6.0:\n\nArea 1.6.0 (Connector/C) 2.0 (native)\nClient flags if/elseif bug: only the first true flag among found_rows, no_schema, compress, ignore_space, local_files, multi_statements, multi_results was applied; multi_statements silently defaulted true in code independent flags; multi_statements default false; multi_results is a no-op (always on)\ncompress=true accepted ArgumentError (compression is not implemented; planned for 2.x)\nlocal_files=true accepted without a handler requires local_infile_handler, otherwise ArgumentError at connect; a server upload request without a configured handler is a ProtocolError\nUnknown keywords silently swallowed ArgumentError\nssl_mode #240: enum collision, SSL_MODE_DISABLED unimplementable five real modes; default :preferred; explicit ssl_mode wins over ssl_enforce/ssl_verify_server_cert/CA-material escalation; contradictions are ArgumentErrors; no plaintext fallback after a failed TLS handshake\nssl_ca + ssl_capath together both applied ArgumentError (Reseau has a single trust-root source); each alone works\nconnect_timeout C socket timeout with platform-dependent meaning one monotonic establishment deadline spanning dial, greeting, TLS, the whole auth exchange, and the charset bootstrap\nread_timeout / write_timeout MYSQL_OPT_READ_TIMEOUT/MYSQL_OPT_WRITE_TIMEOUT: per socket operation same meaning: re-armed before every transport read/write, so a slowly-consumed streaming result never expires while the server keeps answering; expiry closes the connection\nreconnect C auto-reconnect the same contract, made explicit: the command that hits a dead connection reports it (MySQL.Error 2006/2013), and the next command reconnects — only before a send, never mid-command, never inside a transaction; a reconnect that itself fails leaves the connection retryable, not closed\nConnection loss API.Error 2006 \"server has gone away\" / 2013 \"lost connection during query\" the same MySQL.Error codes: 2006 when the server dropped the connection instead of answering, 2013 mid-response; a MySQL 8.0.24+ server that reaps an idle connection (wait_timeout) announces it, and that announcement is reported as MySQL.Error 4031; the connection is closed until reconnect or a new connect\nexecutemultiple first-OK result yielded nothing; later results mutated one cursor (stale lookup, aliased metadata) every result (DML/OK included) is a distinct cursor with immutable metadata and its own OK snapshot; advancing past an unconsumed streaming result drains and invalidates it\nlastrowid read live connection/statement state (sticky) snapshot from the cursor's own OK/terminator (a SELECT cursor reports 0)\nDML cursor length -1 (the C client's sentinel; collect threw) a result-less cursor has length 0 and iterates empty; buffered SELECT cursors report the row count; the outcome is in rows_affected/lastrowid\nDATETIME/TIMESTAMP result type DateTime (milliseconds): sub-millisecond values errored on the text protocol and were silently truncated on the binary one; mysql_date_and_time=true gave DateAndTime, whose text path read DATETIME(1..5) fractions as an unscaled microsecond count (.4 → 4 µs) Timestamp{Second} / Timestamp{Millisecond} / Timestamp{Microsecond} by the column's fsp; every digit is kept, nothing is truncated; DateTime parameters still bind, a Timestamp{Nanosecond} parameter with a sub-microsecond part raises InexactError\nString and BLOB result types String / Vector{UInt8} copies (one allocation per value; TextRow values could alias freed C memory, #206) DataString / DataBytes views of the Julia-owned result buffer: no copy, no allocation per value, and the values stay valid after the row is gone; explicit String/Vector{UInt8} requests still copy\nBIT decoding text: first byte only; binary: little-endian big-endian value of all bytes (≤ 8) in both protocols\nBIT parameters little-endian bitvalue encoding big-endian binary string (matches the decode)\nBool parameters fell through to the MYSQL_TYPE_STRING fallback (untested latent bug) bound as MYSQL_TYPE_TINY\nTIME decoding text parse errored on negative/≥24 h; binary ignored sign and days Dates.Time for 0 ≤ t < 24h, ConversionError otherwise; time_type=Dates.Microsecond opt-in is lossless and signed\nZero dates text special-cased only zero DATETIME; text zero DATE failed; binary mapped zero components to 1970 unified zero_dates policy: :sentinel (default, Date(0) / Timestamp{P}(0, 1, 1)), :missing (widens column types to Union{Missing, T}), :error; partial zero dates (2024-00-05) are ConversionError unless :missing\nBase.isopen mysql_ping round trip local check only; use MySQL.ping(conn) for a round trip\nErrors API.Error/API.StmtError with pointer-only constructors MySQL.Error/MySQL.StmtError keep the same names, field names and types (errno::Cuint, msg) and showerror text, in a real hierarchy (MySQLError → ServerError → Error/StmtError, plus ProtocolError, AuthError, TimeoutError, ConversionError, …), with public constructors and a new sqlstate field\nBuffered memory unbounded buffered results are bounded by max_buffered_bytes (default 256 MiB, per command across all retained result sets incl. row offsets/NULL masks/metadata); exceeding it is a ProtocolError. Streaming stays unbounded by default (max_response_bytes=nothing)\nTransactions lock not held the connection lock is held across DBInterface.transaction(f, conn): other tasks block until commit/rollback\nCleanup/finalizers abandoned C handles depended on Connector/C lifetimes explicit close! or a do-block remains the contract; a dropped connection only enqueues its transport for the timer reaper, and a dropped statement only parks its preallocated id for the next command. Finalizers do no protocol or transport I/O; explicit close, timer reaping, and parked statement close are exactly-once\nConcurrent use not thread-safe connection operations are lock-serialized. One task must consume a streaming cursor; a command from another task drains the pending response and invalidates that cursor instead of overwriting its Julia-owned row bytes. A transaction owns the connection lock until commit or rollback\nMySQL.load one round trip per row; embedded backticks in identifiers were not escaped; debug=true logged row values rows are inserted in multi-row batches (batchsize=1000, bounded by the packet size, max_columns, and the 65535-marker limit); doubles embedded identifier backticks; debug=true logs statements only; debug=:values logs row values","title":"Behavior changes (Fix)"},{"category":"section","location":"migration/#Deprecated-(accepted-with-a-warning;-no-effect)","page":"Migrating from 1.x","text":"data_truncation (no C buffer truncation exists natively)\nnet_buffer_length (buffer sizing is automatic)\nsecure_auth (mysql_old_password is never supported; the option has no effect)\nmulti_results (multiple result sets are always negotiated; the option has no effect)","title":"Deprecated (accepted with a warning; no effect)"},{"category":"section","location":"migration/#Removed-(error-explains-the-replacement)","page":"Migrating from 1.x","text":"charset_dir; charset_name accepts only \"utf8mb4\"\nconnection_handler, plugin_dir (no C plugins to load)\nprotocol=:memory (shared memory transport)","title":"Removed (error explains the replacement)"},{"category":"section","location":"migration/#Temporarily-unavailable-(compatibility-names-are-reserved)","page":"Migrating from 1.x","text":"ssl_cipher, ssl_crl, ssl_crlpath, and passphrase need matching transport support in Reseau. MySQL.jl keeps these option names reserved and reports that they are not available in 2.0; it does not silently ignore recognized option-file forms, and the options are not declared permanently removed.\ncompress=true needs a compressed-packet layer with its own sequence tracking, buffering, and resource limits. It is planned for a later 2.x release.","title":"Temporarily unavailable (compatibility names are reserved)"},{"category":"section","location":"migration/#Added","page":"Migrating from 1.x","text":"ssl_mode (five modes), tls_version, ssl_server_name, get_server_public_key, server_public_key, enable_cleartext_plugin, insecure_cleartext_auth, can_handle_expired_passwords, local_infile_handler, max_local_infile_bytes, zero_dates, time_type, read_env (opt-in MYSQL_TCP_PORT; MYSQL_PWD is never read), max_buffered_bytes, max_response_bytes, max_columns, max_result_sets, max_metadata_bytes, max_preauth_packet, max_auth_rounds, max_auth_bytes, max_session_state_bytes, attrs (connection attributes sent in the handshake), debug (per-packet protocol debug logging), MySQL.ping, MySQL.connection_id, MySQL.server_version, MySQL.server_kind, MySQL.escape_identifier, MySQL.send_long_data!, MySQL.reset_statement!, MySQL.DecimalResult, MySQL.timestamp_type, the re-exported Timestamp, and the batchsize keyword of MySQL.load.","title":"Added"},{"category":"section","location":"migration/#Option-value-types-(2.0)","page":"Migrating from 1.x","text":"Connection-option values are validated against closed type sets (this is also what makes the client compilable with juliac --trim=safe): string options accept String or SubString{String}; integer options accept the standard machine integer types or a decimal string (the public DBInterface.connect method requires an integer or nothing for port); boolean options accept Bool; attrs accepts Vector{Pair{String, String}}; zero_dates accepts a Symbol or String; time_type accepts exactly Dates.Time or Dates.Microsecond. Anything else raises an ArgumentError naming the option (1.x silently accepted, coerced, or ignored some of these).","title":"Option value types (2.0)"},{"category":"section","location":"migration/#Static-compilation-(juliac-trim)","page":"Migrating from 1.x","text":"MySQL.jl 2.0 compiles under juliac --trim=safe; Pkg.test includes a trim workload (test/mysql_trim_workload.jl) that compiles and runs connect, text-protocol execute (buffered and streaming), prepared statements, one-shot parameterized execute, ping, and escape against a scripted in-process server. In a trimmed executable, consume rows with the schema-typed accessor — Tables.getcolumn(row, T, i, name) — the same call schema-aware sinks make. The runtime-schema conveniences (Tables.columntable, untyped row.name access, MySQL.load) build columns from runtime Type values and are not statically resolvable; use them from regular Julia. A custom local_infile_handler (and the IO it returns) is dispatched dynamically and works in a trimmed binary only if its methods were compiled in. The trim workload also exercises abandoned-connection reaping, but leaves all connection deadlines disabled; it does not validate timeout behavior. connect_timeout hangs in trimmed executables with Reseau ≤ 1.4.1 — its deadline-armed dial parked on tasks a trimmed build never ran, fixed in Reseau #151.","title":"Static compilation (juliac --trim)"},{"category":"section","location":"migration/#Security:-what-ssl_mode:preferred-does-and-does-not-give-you","page":"Migrating from 1.x","text":"The default ssl_mode=:preferred matches libmysqlclient, the MySQL CLI, Connector/J, MySqlConnector, and libpq. Its precise guarantees:\n\n:preferred provides confidentiality against passive observers only; it gives no protection against an active man-in-the-middle. Capability stripping by an active attacker removes the opportunistic encryption (the client then continues in plaintext, because the server appears not to support TLS). Full caching_sha2_password / sha256_password authentication over unverified TLS can disclose the password to an active MITM. Only :verify_ca and :verify_identity authenticate the server.\n\nHardening relative to Oracle's documented behavior: after a failed TLS handshake the client never falls back to plaintext, SNI is sent for DNS host names in every TLS mode, and supplying CA material escalates the default to :verify_ca. Cleartext authentication (mysql_clear_password) additionally requires explicit enablement and either :verify_identity or insecure_cleartext_auth=true.","title":"Security: what ssl_mode=:preferred does and does not give you"},{"category":"section","location":"migration/#Not-yet-implemented-(deferred)","page":"Migrating from 1.x","text":"Documented gaps, planned for later 2.x releases — attempting to use them raises a clear error rather than misbehaving:\n\nUnix sockets and Windows named pipes (transport is TCP/TLS). Every host, including \"localhost\", is dialed over TCP; asking for the local transport explicitly (protocol=:socket/:pipe, or named_pipe=true without protocol=:tcp) raises a clear error, and a unix_socket path (keyword or option file) is accepted but unused until the transport exists.\nCompression (compress=true is an ArgumentError), server cursors / COM_STMT_FETCH, query attributes, COM_STMT_BULK_EXECUTE\nMariaDB client_ed25519 / PARSEC / dialog (PAM) authentication (UnsupportedAuthError)\nMYSQL_TYPE_VECTOR result columns (MySQL 9.x; its classic-protocol binary framing is not documented by the vendor sources in scope)\nOUT-parameter interpretation beyond prepared CALL result sets\nPooling, cancellation, DSN parsing\nThe external interop matrix (ProxySQL / TiDB / Vitess / Aurora) is deferred. It is not automated or claimed as validated for 2.0.","title":"Not yet implemented (deferred)"},{"category":"section","location":"#MySQL.jl-Documentation","page":"Home","text":"","title":"MySQL.jl Documentation"},{"category":"section","location":"#Getting-started","page":"Home","text":"MySQL.jl is a client for MySQL and MariaDB servers. Since 2.0 it speaks the MySQL client/server wire protocol natively in Julia (on Reseau.jl TCP/TLS transports) — no C database connector is involved. If you are upgrading from 1.x, see Migrating from 1.x. Install it with:\n\n] add MySQL\n\nConnect through the DBInterface.jl API, which the rest of this page uses:\n\nusing MySQL, DBInterface, Dates   # MySQL re-exports Durations.Timestamp\n\nconn = DBInterface.connect(MySQL.Connection, \"localhost\", \"user\", \"password\"; db=\"mydb\")\n\nEvery host, \"localhost\" included, is dialed over TCP (port 3306 unless port is given). TLS is used whenever the server offers it (ssl_mode=:preferred); see Connection options for the full keyword list.","title":"Getting started"},{"category":"section","location":"#Queries-and-results","page":"Home","text":"There are two ways to run SQL:\n\nDBInterface.execute(conn, sql) runs a statement over the text protocol.\nstmt = DBInterface.prepare(conn, sql); DBInterface.execute(stmt, params) prepares a statement once and executes it any number of times with parameters bound to its ? markers over the binary protocol. DBInterface.execute(conn, sql, params) is the one-shot form (prepare, execute, close).\n\nBoth return a MySQL.Cursor, a Tables.jl row source, so a result can be materialized as DataFrame(cursor), Tables.columntable(cursor), CSV.write(\"out.csv\", cursor), or iterated row by row:\n\nfor row in DBInterface.execute(conn, \"SELECT id, name FROM users\")\n    println(row.id, \": \", row.name)   # row.name, row[:name], row[2]\nend\n\nA row is valid only while it is the cursor's current row (the cursor is a forward-only iterator); collect the values you need before advancing. By default the whole result set is read at execute time (mysql_store_result=true); mysql_store_result=false streams rows as they are iterated, which keeps memory flat for large results but keeps the connection busy until the cursor is exhausted or closed. cursor.rows_affected and DBInterface.lastrowid(cursor) report a DML statement's outcome.\n\nParameters are bound by their Julia type: integers, floats, strings (String, DataString, any AbstractString), bytes (Vector{UInt8}, DataBytes), Date/Time/Timestamp/DateTime, MySQL.Bit, DataDecimals decimals, Bool, and missing/nothing for NULL.\n\nPass a tuple, named tuple, vector, or Tables.AbstractRow for multiple parameters. Values bind to ? markers in iteration order; names do not change that order. Named SQL markers such as :name are not supported. A bare scalar binds one parameter; use (bytes,) to bind a byte vector as one binary value. executemany takes one collection per parameter, as in the example below.\n\nstmt = DBInterface.prepare(conn, \"INSERT INTO users (name, joined) VALUES (?, ?)\")\nDBInterface.execute(stmt, (\"alice\", Date(2026, 1, 2)))\nDBInterface.executemany(stmt, (name=[\"bob\", \"carol\"], joined=[Date(2026, 1, 3), Date(2026, 1, 4)]))\nDBInterface.close!(stmt)\n\nDBInterface.transaction(conn) do ... end wraps the block in START TRANSACTION/COMMIT (ROLLBACK on an exception). DBInterface.executemultiple iterates the result sets of a CALL or of a multi-statement string (multi_statements=true) as distinct cursors. MySQL.load(table, conn, name) creates a table from a Tables.jl source's schema and inserts its rows in batches. Close the connection with DBInterface.close!(conn).","title":"Queries and results"},{"category":"section","location":"#Connection-options","page":"Home","text":"DBInterface.connect(MySQL.Connection, host, user, password; kw...) accepts the keywords below (password=nothing can use an option-file password; \"\" overrides it with an empty password). Unknown keywords raise ArgumentError. Removed and unavailable keywords also raise ArgumentError unless their value is nothing or false.\n\nWhere and how to connect\n\nkeyword default meaning\ndb \"\" default database (USE)\nport 3306 TCP port; omitted/nothing uses file or opt-in environment defaults; 0 explicitly selects 3306\nprotocol :default :tcp, or the deferred :socket/:pipe (they raise a clear error)\nbind nothing local interface/address to connect from\nconnect_timeout nothing positive integer seconds for the whole establishment (dial, TLS, authentication, charset bootstrap)\nread_timeout, write_timeout nothing positive integer seconds per transport read/write; expiry closes the connection\nreconnect false after a command fails on a dead connection, the next command reconnects (never inside a transaction)\ninit_command nothing SQL run after authentication and charset bootstrap; it must also succeed in expired-password sandbox mode\ncharset_name \"utf8mb4\" the only supported character set; the session is bootstrapped to utf8mb4\nattrs client name/version/OS/pid Vector{Pair{String, String}} of connection attributes sent in the handshake\noption_file, option_group nothing a my.cnf/my.ini file (and group besides [client]) supplying host/user/password/port/database/TLS settings\nread_default_file, read_default_group false also read the standard option-file locations\nread_env false let MYSQL_TCP_PORT fill an omitted port (MYSQL_PWD is never read)\ndebug false log every packet at @debug level\n\nServer-side behaviour flags\n\nkeyword default meaning\nmulti_statements false allow \"stmt1; stmt2\" (consume the results with DBInterface.executemultiple)\nfound_rows false rows_affected counts matched rows instead of changed rows\nno_schema, ignore_space false the corresponding server flags\nlocal_files false allow LOAD DATA LOCAL INFILE; requires local_infile_handler\nlocal_infile_handler nothing filename -> IO (or nothing to refuse) called when the server requests a local file\nmax_local_infile_bytes 1 GiB cap on one upload\ncan_handle_expired_passwords false connect in sandbox mode with an expired password for ALTER USER USER() IDENTIFIED BY '…' / SET PASSWORD; normal queries fail with error 1820 until the password is reset\n\nTLS\n\nkeyword default meaning\nssl_mode :preferred :disabled, :preferred (TLS when offered, no fallback after a failed handshake), :required, :verify_ca, :verify_identity; only the last two authenticate the server\nssl_ca / ssl_capath nothing CA bundle file / hashed CA directory (one of them); supplying one raises the default mode to :verify_ca\nssl_cert, ssl_key nothing client certificate and key (mutual TLS)\nssl_server_name host the name used for SNI and certificate verification\ntls_version TLS 1.2 and 1.3 e.g. \"TLSv1.3\"\nssl_enforce, ssl_verify_server_cert nothing true requires TLS / identity verification; false does not lower the mode; contradictions with an explicit keyword ssl_mode are errors\n\nAuthentication (mysql_native_password, caching_sha2_password, sha256_password, and mysql_clear_password are supported)\n\nkeyword default meaning\ndefault_auth supported server default, else caching_sha2_password plugin to answer the handshake with; an auth switch selects the account's plugin\nget_server_public_key false fetch the server's RSA key for caching_sha2/sha256 full authentication over plain TCP\nserver_public_key nothing path of that key in PEM form\nenable_cleartext_plugin false allow mysql_clear_password (PAM/LDAP accounts); default_auth=\"mysql_clear_password\" also enables it\ninsecure_cleartext_auth false allow cleartext authentication without :verify_identity\n\nResult decoding\n\nkeyword default meaning\nzero_dates :sentinel 0000-00-00 values: :sentinel (Date(0) / Timestamp{P}(0, 1, 1)), :missing (dates become Union{Missing, T}), :error\ntime_type Dates.Time TIME columns as Dates.Time (0 ≤ t < 24h) or Dates.Microsecond (signed, up to ±838 h)\n\nLimits (received lengths are checked before growing their payload buffers; outgoing commands are checked after encoding and before sending)\n\nkeyword default meaning\nmax_allowed_packet 16 MiB largest packet sent or accepted\nmax_buffered_bytes 256 MiB retained bytes of one buffered command (nothing = unlimited)\nmax_response_bytes nothing cap on a whole response, streamed rows included\nmax_columns, max_result_sets, max_metadata_bytes 4096, 1024, 16 MiB columns per result or parameters per prepared statement; result sets and metadata bytes per command\nmax_preauth_packet, max_auth_rounds, max_auth_bytes min(1 MiB, max_allowed_packet), 8, 64 KiB connection-phase bounds\nmax_session_state_bytes 1 MiB session-state blocks in an OK packet, including command responses\n\nDeprecated 1.x keywords (data_truncation, net_buffer_length, secure_auth, multi_results) are accepted with a warning and have no effect; see the migration guide for removed and not-yet-available ones.","title":"Connection options"},{"category":"section","location":"#Option-file-rules","page":"Home","text":"Option files are opt-in. option_file selects one file. read_default_file=true or read_default_group=true also reads the default locations. option_group without option_file selects those locations as well. On Unix these are /etc/my.cnf, /etc/mysql/my.cnf, and ~/.my.cnf, in that order. On Windows they are my.ini and my.cnf in %WINDIR%, then in C:\\. The explicit file is read last. A missing explicit file is an error; missing default files are skipped. World-writable files on Unix and .mylogin.cnf login-path files are skipped with a warning.\n\nThe reader selects [client] and option_group, with case-insensitive group names. The last occurrence of an option in file order wins, including across selected groups. Quoted values, MySQL backslash escapes, # comments, and full-line ; comments are supported. !include, !includedir, and ?includedir raise an error. Unknown keys are ignored. This is a subset of the MySQL option-file format.\n\nSupported file keys are host, user, password, port, database, connect-timeout, default-character-set, protocol, bind-address, socket, tls-version, ssl-mode, ssl-ca, ssl-capath, ssl-cert, and ssl-key. Underscores can replace hyphens. socket is accepted but unused. ssl-cipher, ssl-crl, and ssl-crlpath raise an error because these settings are unavailable. compress also raises an error unless its value is 0, OFF, or FALSE (case-insensitive).\n\nNonempty positional host/user values and non-nothing keywords override file values. An empty host/user allows file fallback. An empty password or database overrides the file with an empty value. MYSQL_TCP_PORT is used only with read_env=true and when neither a keyword nor a file supplies the port. MYSQL_PWD is never read.\n\nFor TLS, keyword ssl_mode wins over file ssl-mode. Without that keyword, ssl_verify_server_cert=true selects :verify_identity; ssl_enforce=true requires at least :required and preserves a stronger file mode. Otherwise the file mode wins. Without any mode selection, a CA selects :verify_ca; the remaining default is :preferred.","title":"Option-file rules"},{"category":"section","location":"#Types","page":"Home","text":"Result columns decode to the Julia types below (Union{Missing, T} unless the column is NOT NULL; UNSIGNED integer columns map to the unsigned counterpart):\n\nMySQL Julia\nTINYINT, SMALLINT, MEDIUMINT/INT, BIGINT Int8, Int16, Int32, Int64\nFLOAT, DOUBLE Float32, Float64\nDECIMAL/NUMERIC MySQL.DecimalResult (exact, all 65 digits)\nBIT(n) MySQL.Bit\nDATE, TIME Date, Time (or Microsecond with time_type)\nDATETIME/TIMESTAMP Timestamp{Second}; with fsp 1–3 Timestamp{Millisecond}, 4–6 Timestamp{Microsecond}\nYEAR Clong (unsigned)\nCHAR/VARCHAR/TEXT, BINARY/VARBINARY, ENUM, SET, JSON DataString\nBLOB, GEOMETRY DataBytes\n\nDataString and DataBytes are DataStrings.jl's compact string and byte values (the Arrow Utf8View/BinaryView layout): a value of up to 12 bytes is stored inline, a longer one references the cursor's row buffer, so decoding a result copies no bytes and allocates nothing per value. DataString <: AbstractString behaves like String (equality, hashing, ordering, iteration, String(s) to copy out); DataBytes <: AbstractVector{UInt8} likewise (Vector{UInt8}(b) copies). A long value keeps the buffer it references alive — the whole result of a buffered cursor, or the arena a streaming cursor read its row into. Arenas have a 64 KiB target; a row can grow an arena past that size. String(s)/Vector{UInt8}(b) detaches a value, and DataStrings.materialize(column) detaches a whole column (as returned by Tables.columntable or a DataFrame), copying every value out to a String or Vector{UInt8} and keeping missings. Requesting String/Vector{UInt8} explicitly through the typed accessor (Tables.getcolumn(row, String, i, name)) still returns a copy. BINARY/VARBINARY keep the 1.x string mapping; their bytes need not be valid UTF-8.\n\nTimestamp{P} is Durations.jl's Int64 count since the Unix epoch at resolution P (the type proposed for the Julia 1.14 Dates stdlib, which it becomes automatically there); MySQL re-exports it. It is an AbstractDateTime: Dates.year(ts), DateTime(ts), Date(ts), Time(ts), arithmetic with periods, and comparisons with DateTime/Date all work, and every MySQL value is represented exactly. Construct one with Timestamp{Microsecond}(2024, 2, 29, 13, 14, 15, 250, 500).\n\nMySQL.juliatype computes the mapping for a wire type and its flags.","title":"Types"},{"category":"section","location":"#Errors","page":"Home","text":"Server errors are thrown as MySQL.Error (or MySQL.StmtError from prepared-statement operations) with errno, msg, and sqlstate fields. A connection the server dropped reports 4031 when MySQL supplies an idle-disconnect ERR, or the classic client codes 2006 (\"MySQL server has gone away\") and 2013 (\"Lost connection to MySQL server during query\") when the transport closes. Protocol and server exceptions derive from MySQL.MySQLError: besides the server errors there are ProtocolError (the byte stream violated the protocol or a limit; the connection is closed), TimeoutError, AuthError, TLSNegotiationError, ConversionError (a value cannot be represented by the column's Julia type), and LocalInfileRefused. Invalid options and stale row access can raise ArgumentError; transport errors can also propagate.","title":"Errors"},{"category":"section","location":"#API-reference","page":"Home","text":"","title":"API reference"},{"category":"section","location":"#Connections-and-results","page":"Home","text":"","title":"Connections and results"},{"category":"section","location":"#Driver-helpers","page":"Home","text":"","title":"Driver helpers"},{"category":"section","location":"#Value-types","page":"Home","text":"","title":"Value types"},{"category":"section","location":"#Errors-2","page":"Home","text":"","title":"Errors"},{"category":"section","location":"#Internal-implementation","page":"Home","text":"MySQL.Protocol is documented for maintainers. It is not part of the stable user API.","title":"Internal implementation"},{"category":"type","location":"#MySQL.Connection","page":"Home","text":"MySQL.Connection\n\nA MySQL connection. Obtain one with DBInterface.connect(MySQL.Connection, host, user, password; kw...); see MySQL.ConnectOptions for the accepted keywords (removed 1.x ones explain why they fail). Operations are serialized by the connection lock. The first task to consume a streaming cursor owns it; a transaction is owned by the task that starts it.\n\n\n\n\n\n","title":"MySQL.Connection"},{"category":"type","location":"#MySQL.Statement","page":"Home","text":"MySQL.Statement\n\nA prepared statement on the native backend, from DBInterface.prepare(conn, sql). Execute it with DBInterface.execute(stmt, params); close it with DBInterface.close!(stmt) (the COMSTMTCLOSE is deferred to the next command, never sent from a finalizer).\n\n\n\n\n\n","title":"MySQL.Statement"},{"category":"type","location":"#MySQL.Cursor","page":"Home","text":"MySQL.TextCursor{buffered}\nMySQL.BinaryCursor{buffered}\n\nThe cursor returned by DBInterface.execute: TextCursor for execute(conn, sql) (text protocol), BinaryCursor for execute(stmt, params) (binary protocol). It iterates rows and satisfies the Tables.jl row interface. buffered=true (mysql_store_result=true, the default) reads the whole result set at execute time under max_buffered_bytes; buffered=false streams rows on each iterate and ties up the connection until exhausted. A row is valid only while it is the cursor's current row; the values taken from it (including DataString/DataBytes views) stay valid.\n\n\n\n\n\n","title":"MySQL.Cursor"},{"category":"type","location":"#MySQL.ConnectOptions","page":"Home","text":"ConnectOptions\n\nValidated, fully resolved connection parameters (see ConnectOptions(host, user, password; kw...)).\n\n\n\n\n\n","title":"MySQL.ConnectOptions"},{"category":"function","location":"#DBInterface.connect","page":"Home","text":"DBInterface.connect(MySQL.Connection, host, user, passwd=nothing; db=nothing, port=nothing, kw...)\n\nConnects to a MySQL server. Keywords are the 1.x connection options plus ssl_mode=:preferred, get_server_public_key, tls_version, zero_dates, time_type, local_infile_handler, max_buffered_bytes, …; see MySQL.ConnectOptions. An omitted db/port falls back to the option files' database/port (when option files are read), like host/user/password.\n\n\n\n\n\n","title":"DBInterface.connect"},{"category":"function","location":"#DBInterface.close!","page":"Home","text":"DBInterface.close!(conn)\n\nSends COM_QUIT (best effort) and closes the transport. Idempotent; unfinished streaming cursors become invalid. Buffered cursors retain their own bytes and remain readable.\n\n\n\n\n\nDBInterface.close!(c::MySQL.Cursor)\n\nCloses c and invalidates its current row. Further iteration yields no rows, for both buffered and streaming cursors. Repeated calls have no effect.\n\nIf c still owns a pending response, discards the remaining rows and result sets for its command.\n\n\n\n\n\nDBInterface.close!(stmt::MySQL.Statement)\n\nCloses the prepared statement. The COMSTMTCLOSE is parked and sent before the next command (never from a finalizer). Idempotent.\n\n\n\n\n\n","title":"DBInterface.close!"},{"category":"function","location":"#DBInterface.execute","page":"Home","text":"DBInterface.execute(conn::MySQL.Connection, sql; mysql_store_result=true) -> TextCursor\n\nRuns sql with the text protocol and returns a cursor over the first result. With mysql_store_result=false rows are streamed (the connection is busy until the cursor is exhausted or closed). Further results of a multi-statement or CALL response are discarded by the next operation; use DBInterface.executemultiple to consume them. Passing params prepares, executes and returns a binary-protocol cursor bound to a one-shot statement.\n\n\n\n\n\nDBInterface.execute(stmt::MySQL.Statement, params=(); mysql_store_result=true) -> BinaryCursor\n\nExecutes the prepared statement with params bound as the ? markers and returns a binary-protocol cursor. A tuple, named tuple, vector, or Tables.AbstractRow supplies values in iteration order; names do not select SQL parameters. A bare scalar binds one parameter. Wrap a binary byte vector as (bytes,) to bind it as one value. Named SQL markers such as :name are not supported. mysql_store_result=false streams rows (the connection is busy until the cursor is exhausted or closed).\n\n\n\n\n\n","title":"DBInterface.execute"},{"category":"function","location":"#DBInterface.executemultiple","page":"Home","text":"DBInterface.executemultiple(stmt::MySQL.Statement, params=(); kw...) -> Cursors\n\nIterates every result set of a prepared CALL (or multi-result statement) as a distinct binary cursor, like the connection-level executemultiple.\n\n\n\n\n\n","title":"DBInterface.executemultiple"},{"category":"function","location":"#DBInterface.prepare","page":"Home","text":"DBInterface.prepare(conn::MySQL.Connection, sql) -> Statement\n\nPrepares sql on the server and returns a Statement.\n\n\n\n\n\n","title":"DBInterface.prepare"},{"category":"function","location":"#DBInterface.transaction","page":"Home","text":"DBInterface.transaction(f, conn)\n\nRuns f() inside START TRANSACTION / COMMIT (or ROLLBACK when f throws) and returns f()'s value. The connection lock is held for the whole callback: other tasks block until the transaction ends, so f must not wait on tasks that need this connection.\n\n\n\n\n\n","title":"DBInterface.transaction"},{"category":"function","location":"#DBInterface.lastrowid","page":"Home","text":"DBInterface.lastrowid(c::MySQL.Cursor)\n\nThe last_insert_id the server reported in this cursor's own OK packet (the DML result, or the result-set terminator), not the connection's current state.\n\n\n\n\n\n","title":"DBInterface.lastrowid"},{"category":"function","location":"#MySQL.ping","page":"Home","text":"MySQL.ping(conn) -> Bool\n\nCOM_PING round trip; throws when the connection is unusable.\n\n\n\n\n\n","title":"MySQL.ping"},{"category":"function","location":"#MySQL.connection_id","page":"Home","text":"MySQL.connection_id(conn) -> Int\n\nThe server-side id of this connection (what CONNECTION_ID() returns), e.g. to cancel a long-running statement with KILL QUERY <id> from another connection.\n\n\n\n\n\n","title":"MySQL.connection_id"},{"category":"function","location":"#MySQL.server_version","page":"Home","text":"MySQL.server_version(conn) -> VersionNumber\n\nThe server version announced in the greeting (a MariaDB 5.5.5- prefix is stripped), for gating on server features; MySQL.server_kind(conn) tells :mysql from :mariadb.\n\n\n\n\n\n","title":"MySQL.server_version"},{"category":"function","location":"#MySQL.server_kind","page":"Home","text":"MySQL.server_kind(conn) -> Symbol\n\n:mysql, :mariadb, :tidb, or :vitess, detected from the greeting.\n\n\n\n\n\n","title":"MySQL.server_kind"},{"category":"function","location":"#MySQL.escape","page":"Home","text":"MySQL.escape(conn, str) -> String\n\nEscapes str for use inside a single-quoted SQL literal on this connection's character set (utf8mb4): \\, ', \", NUL, newline, carriage return and Control-Z are backslash-escaped; under the session's NO_BACKSLASH_ESCAPES mode only ' is doubled.\n\n\n\n\n\n","title":"MySQL.escape"},{"category":"function","location":"#MySQL.escape_identifier","page":"Home","text":"MySQL.escape_identifier(name) -> String\n\nBacktick-quotes an identifier, doubling embedded backticks.\n\n\n\n\n\n","title":"MySQL.escape_identifier"},{"category":"function","location":"#MySQL.send_long_data!","page":"Home","text":"MySQL.send_long_data!(stmt, parameter_number, data)\n\nSends one copied string or byte chunk for the zero-based prepared-statement parameter number. Repeated calls append chunks. The next execute omits that parameter's inline value and retains the copied chunks until its first response, so a 1615 or reconnect re-prepare can replay them.\n\n\n\n\n\n","title":"MySQL.send_long_data!"},{"category":"function","location":"#MySQL.reset_statement!","page":"Home","text":"MySQL.reset_statement!(stmt)\n\nResets a prepared statement's accumulated long data. The statement id and cached parameter signature remain valid when the session generation did not change.\n\n\n\n\n\n","title":"MySQL.reset_statement!"},{"category":"function","location":"#MySQL.load","page":"Home","text":"MySQL.load(table, conn, name; append=true, quoteidentifiers=true, limit=typemax(Int64), batchsize=1000, createtableclause=nothing, coltypes=Dict(), columnsuffix=Dict(), auto_increment_primary_key_name=nothing, debug=false)\ntable |> MySQL.load(conn, name; kw...)\n\nLoads a Tables.jl source table into the table name of the database conn is connected to, and returns the (quoted) table name.\n\nIt first detects the Tables.Schema of the table source and generates a CREATE TABLE statement with the appropriate column names and types. If no table name is provided, one will be autogenerated, like mysql_xxxxx. The CREATE TABLE clause can be provided manually by passing the createtableclause keyword argument (default \"CREATE TABLE IF NOT EXISTS\" with append=true, else \"CREATE TABLE\"), which would allow specifying a temporary table. With append=false the existing rows are deleted before the new ones are inserted. Column types can be overridden by providing the coltypes keyword argument as a Dict of column name (given as a Symbol) to a string of the SQL type. This allows, for example, using a LONGBLOB instead of BLOB for large binary data by doing coltypes=Dict(:Photo => \"LONGBLOB\"). Column definitions can also be enhanced by providing arguments to columnsuffix as a Dict of column name (given as a Symbol) to a string of the enhancement that will come after name and type like [column name] [column type] enhancements. This allows, for example, specifying the charset of a string column by doing something like columnsuffix=Dict(:Name => \"CHARACTER SET utf8mb4\"). auto_increment_primary_key_name adds an INT AUTO_INCREMENT PRIMARY KEY column of that name in front of the source columns.\n\nRows are inserted inside one transaction with prepared multi-row INSERT statements of up to batchsize rows each (fewer when a batch would approach the packet limit, and at most 65535 bound values per statement, further bounded by max_columns); limit stops after that many rows. The packet budget uses the client's max_allowed_packet option; set it no higher than the server's value. Byte vectors are copied before advancing the source.\n\ndebug=true logs the generated statements without row values; debug=:values also logs each inserted row's values.\n\nDo note that databases vary wildly in requirements for CREATE TABLE and column definitions so it can be extremely difficult to load data generically. You may just need to tweak some of the provided keyword arguments, but you may also need to execute the CREATE TABLE and INSERT statements yourself. If you run into issues, you can open an issue and we can see if there's something we can do to make it easier to use this function.\n\n\n\n\n\n","title":"MySQL.load"},{"category":"function","location":"#MySQL.juliatype","page":"Home","text":"MySQL.juliatype(field_type, notnullable, isunsigned, isbinary, decimals=6) -> Type\n\nThe Julia type a result column decodes to, given its wire type, flags, and fractional precision: unsigned integer widening, binary BLOB (DataBytes) vs text (DataString), DATETIME/TIMESTAMP as Timestamp{P} per decimals (see MySQL.timestamp_type), exact DataDecimals values for DECIMAL, and Union{Missing, T} for nullable columns.\n\n\n\n\n\njuliatype(def::Protocol.ColumnDef, opts::ResultOptions) -> Type\n\nThe column's Julia type: MySQL.juliatype applied to the wire type, flags, and fractional precision (decimals), then the decoding policies: time_type, and zero_dates=:missing widening every date column to Union{Missing, T} regardless of NOT NULL.\n\n\n\n\n\n","title":"MySQL.juliatype"},{"category":"type","location":"#MySQL.Bit","page":"Home","text":"MySQL.Bit\n\nThe value of a BIT(n) column (n ≤ 64): the big-endian value of all bytes the server sent, stored in bits::UInt64. (MySQL.API.Bit before 2.0.)\n\n\n\n\n\n","title":"MySQL.Bit"},{"category":"type","location":"#MySQL.DecimalResult","page":"Home","text":"MySQL.DecimalResult\n\nThe type DECIMAL/NUMERIC columns decode to: DataDecimals.DecimalValue{DataDecimals.Int256}, which holds all 65 digits and the column's scale exactly.\n\n\n\n\n\n","title":"MySQL.DecimalResult"},{"category":"function","location":"#MySQL.timestamp_type","page":"Home","text":"MySQL.timestamp_type(decimals) -> Type{<:Timestamp}\n\nThe Timestamp{P} a DATETIME/TIMESTAMP column with decimals fractional-second digits (its fsp, 0–6) decodes to: Timestamp{Second} for 0, Timestamp{Millisecond} for 1–3, Timestamp{Microsecond} for 4–6. Every MySQL value is represented exactly.\n\n\n\n\n\n","title":"MySQL.timestamp_type"},{"category":"type","location":"#MySQL.MySQLError","page":"Home","text":"MySQLError\n\nRoot of the native backend's exception hierarchy.\n\nServerError (Error, StmtError): a server ERR or a client connection-loss error\nProtocolError: invalid protocol state, bytes, or a resource limit; wire faults close the connection\nAuthError / UnsupportedAuthError: authentication policy or plugin problems\nTimeoutError: a deadline expired\nConversionError: a wire value cannot be represented by the requested Julia type\nTLSNegotiationError: the TLS handshake failed while establishing the connection\nLocalInfileRefused: the LOCAL INFILE handler declined a server request\n\n\n\n\n\n","title":"MySQL.MySQLError"},{"category":"type","location":"#MySQL.Error","page":"Home","text":"Error(errno, msg, sqlstate=\"\")\n\nServer ERR or client connection-loss error. errno::Cuint and msg keep the field names and types of the Connector/C-backed MySQL.API.Error; sqlstate is new.\n\n\n\n\n\n","title":"MySQL.Error"},{"category":"type","location":"#MySQL.StmtError","page":"Home","text":"StmtError(errno, msg, sqlstate=\"\")\n\nServer ERR packet raised by prepared-statement operations (distinct type on purpose so 1.x-style @test_throws MySQL.StmtError dispatch keeps working; the 1.x name was MySQL.API.StmtError).\n\n\n\n\n\n","title":"MySQL.StmtError"},{"category":"module","location":"#MySQL.Protocol","page":"Home","text":"MySQL.Protocol\n\nNative implementation of the MySQL client/server wire protocol on top of Reseau transports: constants generated from the server headers, bounded codecs, packet framing with reassembly, the phase machine, handshake and capability negotiation, authentication plugins (mysql_native_password, caching_sha2_password, sha256_password, mysql_clear_password) with OpenSSL-backed RSA-OAEP, STARTTLS, generic responses, column definitions, text and binary row scanning, and the command/response framing of COMQUERY, the COMSTMT_* family, LOCAL INFILE and the simple commands. It has no DBInterface/Tables dependency; the MySQL driver layer (value decoding, connections, cursors, statements) builds on it. See docs/protocol-notes.md.\n\n\n\n\n\n","title":"MySQL.Protocol"}]
}
